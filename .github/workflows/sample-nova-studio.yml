name: Sample NOVA Image Studio

# Manual QA: preloads the Qwen-Image studio weights on CPU, then renders image
# samples (text-to-image, avatar + product composition) and a full UGC product
# video through the LTX engine. Spends GPU time only when run.
on:
  workflow_dispatch:
    inputs:
      video:
        description: "Also render the UGC video from the composed keyframe"
        default: "true"
        required: false
      duration:
        description: "Video seconds for sample_ltx_local (5, 10 or 15)"
        default: "10"
        required: false
      entrypoint:
        description: "sample_studio (EN UGC), sample_ugc_pt (PT UGC), sample_ltx_local (own-GPU LTX) or sample_free_image_models"
        default: "sample_studio"
        required: false

permissions:
  contents: read

jobs:
  sample:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
      MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install Modal
        run: python -m pip install --upgrade "modal>=1.4,<2"
      - name: Preload studio weights on CPU
        if: inputs.entrypoint != 'sample_free_image_models'
        run: modal run infra/modal/nova_video_worker.py::preload_studio
      - name: Render studio samples
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ inputs.entrypoint }}" = "sample_ugc_pt" ]; then
            modal run infra/modal/nova_video_worker.py::sample_ugc_pt
          elif [ "${{ inputs.entrypoint }}" = "sample_ltx_local" ]; then
            modal run infra/modal/nova_video_worker.py::sample_ltx_local --duration "${{ inputs.duration }}" --skip-ugc
          elif [ "${{ inputs.entrypoint }}" = "sample_free_image_models" ]; then
            modal run infra/modal/nova_video_worker.py::sample_free_image_models
          else
            if [ "${{ inputs.video }}" = "true" ]; then flag="--video"; else flag="--no-video"; fi
            modal run infra/modal/nova_video_worker.py::sample_studio $flag
          fi
      - name: Store samples
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nova-studio-samples
          path: samples/
